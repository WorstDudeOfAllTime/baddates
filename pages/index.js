import Head from 'next/head';
import styles from '../styles/Home.module.css';
import { useState, useEffect } from 'react';
import Geocode from 'react-geocode';
import Map from '../components/Map';
import OverlayBox from '../components/OverlayBox';
Geocode.setApiKey(process.env.NEXT_PUBLIC_GM_API);
Geocode.setLanguage('en');
export default function Home({ allDates }) {
  const getAddress = async (long, lat) => {
    try {
      const response = await Geocode.fromLatLng(lat, long);
      console.log(response.results[0].formatted_address);
    } catch (err) {
      console.log(err);
    }
  };
  const [story, setStory] = useState('');
  const [newStory, setNewStory] = useState('');
  const [markers, setMarkers] = useState(allDates.data);
  const [isLoaded, setIsLoaded] = useState(false);
  const [theAddress, setTheAddress] = useState(null);
  const [overlay, setOverlay] = useState(true);
  const [locationID, setLocationID] = useState('');
  const [commentList, setCommentList] = useState(null);
  const [center, setCenter] = useState({
    lat: 39.9526,
    lng: -75.1652,
  });
  const [newComments, setNewComments] = useState(false);
  return (
    <div className={styles.container}>
      <Head>
        <title>Google API Test</title>
        <meta name="description" content="Generated by create next app" />
      </Head>
      {overlay && (
        <div className={`flexCentCol ${styles.accept}`}>
          <div className={`flexCentCol ${styles.discBox}`}>
            <p>
              Disclaimer: While badDates is intended to be humorous and the
              posts will be moderated to some degree, please proceed with
              caution!
            </p>
            <div className={styles.buttonBox}>
              <button
                onClick={() => {
                  setOverlay(false);
                }}
              >
                Enter!
              </button>

              <a href="https://www.reddit.com/r/Eyebleach/">
                <button>GET ME OUTTA HERE!</button>
              </a>
            </div>
          </div>
        </div>
      )}
      <OverlayBox
        newStory={newStory}
        setNewStory={setNewStory}
        story={story}
        theAddress={theAddress}
        setTheAddress={setTheAddress}
        setCenter={setCenter}
        center={center}
        locationID={locationID}
        commentList={commentList}
        setNewComments={setNewComments}
        newComments={newComments}
      />
      {isLoaded ? (
        <h1 style={{ fontSize: '100px' }}>LOADING</h1>
      ) : (
        <Map
          center={center}
          setCenter={setCenter}
          data={markers}
          setStory={setStory}
          setLocationID={setLocationID}
          setCommentList={setCommentList}
          newComments={newComments}
        />
      )}
    </div>
  );
}

export async function getServerSideProps() {
  let response = await fetch('http:/localhost:3000/api/finddates');
  let allDates = await response.json();
  console.log(allDates);
  return {
    props: { allDates },
  };
}
